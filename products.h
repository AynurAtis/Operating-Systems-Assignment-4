//creating product struct

struct product{
char product_name[30]; // (phone, tshirt, coke etc.)
char product_type[30]; //(electronicDevice, clothing, market etc.)
int price; // (can be integer between 1-10000)
};
typedef struct product Product;
